{
  "name": "newsapi",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -i ./static/css/input.css -o ./static/css/main.css --minify",
    "dev": "tailwindcss -i ./static/css/input.css -o ./static/css/main.css --watch",
    "start": "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "tailwindcss": "^3.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
